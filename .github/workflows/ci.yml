name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    name: Build and test
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
